# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude Code local settings (machine-specific permissions — not for VCS)
.claude/settings.local.json

# Go
bin/
*.exe
*.test
vendor/
go.work.sum
# Compiled chaincode binaries (go build in each channel subdirectory)
contracts/channel1-signatures/channel1-signatures
contracts/channel2-consent/channel2-consent
contracts/channel3-access/channel3-access
contracts/contracts

# Swift / Xcode
build/
xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM
Pods/
.build/
# SentencePiece xcframework (built from scripts/build-sentencepiece-ios.sh)
ios/Frameworks/

# Secrets — never commit
*.pem
*.key
*.p12
*.pfx
*.crt
.env
.env.*
secrets.yaml
secrets.json

# Terraform (future)
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# IDE
.idea/
.vscode/
*.swp
sentencepiece/
